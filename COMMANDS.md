# Hermes 커맨드 사용법

## Reasoning 수준 설정

### 전역 설정

```text
/reasoning high --global
```

Reasoning 수준을 `high`로 설정하고 전역 설정에 저장합니다. 이후 새 세션에도 적용됩니다.

### 현재 세션에만 설정

```text
/reasoning high
```

현재 세션의 Reasoning 수준만 `high`로 설정합니다. 전역 설정은 변경하지 않습니다.
